import 'package:flutter/material.dart';

class PublishedArticlesSection extends StatelessWidget {
  const PublishedArticlesSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
